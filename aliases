List of preferred common Aliases: 

##git
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short #this is the preferred log display

##unix
  type = cat-file -t
  dump = cat-file -p
